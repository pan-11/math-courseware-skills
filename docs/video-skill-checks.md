# 视频Skill扩展与验证记录

Date: 2026-09-12
Version: 0.2.0
Status: Skill流程与离线检查通过；真实首片与课堂播放待验证。

## 改动与职责

新增[math-courseware-video](../skills/math-courseware-video/SKILL.md)及三份职责内引用：制作流程、可填写的材料模板、文件交接与恢复。总入口接入视频；plan从粗情境形成共享故事，video继续展开剧本、声音、分镜、操作包、小样、剪辑与回填；pages及documents按实际采用的视频同步。

共有七个Skill。README与Codex/WorkBuddy安装提示词同步模块清单和0.2.0来源检查。现有18个控制命令、运行时源码及schema保持不变，未增加视频API、配音或剪辑器，也未自动嵌入PPT视频。视频独立清单由Skill按当前可用文件工具执行，现有collect不收视频且依赖锁定课件页面。

## 独立应用检查

按writing-skills先运行旧版入口，再由三个独立代理分别执行真实形式的合成请求；主代理读取实际输出核对。未给代理预期答案，未让其读取作者计划或其他案例答案。

| 案例 | 实际结果 | 结论 |
|---|---|---|
| 旧版基线：仅森林文具店情境 | 能补故事和资产草案，但明确停在“视频Skill以后单独设计”的边界 | 缺少后续完整制作入口 |
| 新版：只有情境，没有故事/图片/PPT | 给同情境候选与具体推荐稿；价格标为自拟；完整声音、教师接话、镜头、最小素材提示词和下一步齐全 | 通过粗情境入口检查 |
| 新版：定稿台词16秒，被要求做10秒 | 保留全文；建议10秒动态素材加静帧容纳完整声音；4珠3抽屉由可控层表现；文件与平台缺项明示 | 通过续作、声音与数学边界检查 |
| 新版：只有成片文件名，台词把“至少”改成“正好”并指定位置 | 不登记成片通过；指出两种语义差异；给修订及三类教学段落草稿；没有虚构页码/媒体信息 | 通过回填边界检查 |

原始输入、输出及逐项审阅在开发目录`tests/runs/video-skill-20260912/`保留，不进入发布包。应用案例通过不能证明任何视频模型的画质或稳定性。

## 离线验证

- `python -B -X utf8 -m unittest discover -s tests -p 'test_*.py' -v`：最终50项全部通过，0失败/错误/跳过，39.782秒。原有47项加3项视频交接的现有状态行为检查。
- 新检查验证：无页面/图片可注册并恢复脚本；video_id变更影响引用页面与产物，保留无关页面；故事重新批准不能把旧视频产物清为有效。
- `tests/validate_skills.py --validator <系统quick_validate.py>`：七入口官方格式通过，元数据与39处Skill内部相对引用通过，Python语法通过。
- `courseware.py doctor`：Python3.12.14、五个文档依赖、OfficeCLI、WPS及楷体路径可发现；实际显示验证仍为false。
- `courseware.py validate --project <合成video-state项目>`：ok=true，0页面/资产也能继续；这不代表实际教学或视觉通过。
- `git -c core.whitespace=cr-at-eol diff --check`：通过；未修改旧运行时、生图配置或旧版发布文件。

首次全量检查有1项原有PPT编辑测试报`System.Private.Xml, Version=10.0.0.0`缺失；当时可读OfficeCLI版本为1.0.149。未改代码、安装依赖或系统配置，原失败项单独复跑及随后全量50项均通过。异常未再次复现，根因未确定；首轮日志与最终日志分别保留，没有把首轮失败隐去或通过跳过测试处理。

## 交付与实际边界

0.2.0包包含七个Skill、共用脚本、README、安装提示词及验证摘要，排除来源原文、案例输出、课程输入、缓存、字体和凭据。0.1.0包与清单保留。打包工具核对完整文件清单、包内引用、ZIP完整性及每文件源/包SHA256；最终证据写入随包manifest和外部release-manifest-0.2.0.json。

本轮未安装全局Skill、未推送GitHub，未生成图片/视频/音频、没有真实剪辑回传、未进行可画或WPS课堂播放。下一步用一段已定教学任务做实际小样，验证参考图一致性、声音长度、数学可控层、剪辑接续与课堂播放；不把“操作包齐全”称为“稳定出片已验证”。
